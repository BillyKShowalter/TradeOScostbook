export interface Badge404Props {
  className?: string;
  style?: React.CSSProperties;
}

/**
 * The 404 · TRADE · OS monospace logo badge. Pairs with <Wordmark/> in the
 * full lockup. Never recolor the segments or drop "404".
 *
 * @startingPoint section="Brand" subtitle="404 · TRADE · OS logo badge" viewport="220x40"
 */
export function Badge404(props: Badge404Props): JSX.Element;
