One-line: The monospace `404 · TRADE · OS` logo badge that always accompanies the wordmark; use it anywhere the brand mark appears (nav, hero, cards, CTAs).

```jsx
<Badge404 />
```

Notes:
- "404" is part of the brand name — never drop it or recolor the segments.
- Stacks above `<Wordmark/>` in tight vertical nav lockups.
