One-line: CNC-reticle corner brackets that frame any `position:relative` box — the signature "targeting" motif; commonly faded in on card hover.

```jsx
<div style={{ position: "relative" }}>
  <CornerBrackets size={14} />
  …content…
</div>
```

Notes: decorative + `aria-hidden`. Parent must be `position:relative`.
